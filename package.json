{
    "name": "hamikdash-website",
    "version": "1.0.0",
    "description": "Hamikdash e-commerce website",
    "scripts": {
        "build": "cd frontend && npm install && npm run build",
        "start": "cd backend && npm install && npm start",
        "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\""
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    },
    "dependencies": {
        "bcrypt": "^6.0.0",
        "dompurify": "^3.3.3"
    }
}
